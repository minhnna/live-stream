entity LiveStreamInfo {
	videoId String,
    aiResult  String,
    result String
}

enum RESULT {
    OK, NG, NC, ERROR
}

// Set pagination options
// paginate Card with infinite-scroll
paginate LiveStreamInfo with pagination

dto * with mapstruct

// Set service options to all except few
service all with serviceImpl
